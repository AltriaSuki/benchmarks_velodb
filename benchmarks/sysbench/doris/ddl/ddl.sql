CREATE TABLE IF NOT EXISTS `sbtest1` (
    `id` INT NOT NULL,
    `k` INT NULL,
    `c` VARCHAR(120) NULL,
    `pad` VARCHAR(60) NULL
) ENGINE=OLAP
UNIQUE KEY(`id`)
COMMENT 'OLAP'
DISTRIBUTED BY HASH(`id`) BUCKETS 3;

CREATE TABLE IF NOT EXISTS `sbtest2` (
    `id` INT NOT NULL,
    `k` INT NULL,
    `c` VARCHAR(120) NULL,
    `pad` VARCHAR(60) NULL
) ENGINE=OLAP
UNIQUE KEY(`id`)
COMMENT 'OLAP'
DISTRIBUTED BY HASH(`id`) BUCKETS 3;

CREATE TABLE IF NOT EXISTS `sbtest3` (
    `id` INT NOT NULL,
    `k` INT NULL,
    `c` VARCHAR(120) NULL,
    `pad` VARCHAR(60) NULL
) ENGINE=OLAP
UNIQUE KEY(`id`)
COMMENT 'OLAP'
DISTRIBUTED BY HASH(`id`) BUCKETS 3;

CREATE TABLE IF NOT EXISTS `sbtest4` (
    `id` INT NOT NULL,
    `k` INT NULL,
    `c` VARCHAR(120) NULL,
    `pad` VARCHAR(60) NULL
) ENGINE=OLAP
UNIQUE KEY(`id`)
COMMENT 'OLAP'
DISTRIBUTED BY HASH(`id`) BUCKETS 3;

CREATE TABLE IF NOT EXISTS `sbtest5` (
    `id` INT NOT NULL,
    `k` INT NULL,
    `c` VARCHAR(120) NULL,
    `pad` VARCHAR(60) NULL
) ENGINE=OLAP
UNIQUE KEY(`id`)
COMMENT 'OLAP'
DISTRIBUTED BY HASH(`id`) BUCKETS 3;

CREATE TABLE IF NOT EXISTS `sbtest6` (
    `id` INT NOT NULL,
    `k` INT NULL,
    `c` VARCHAR(120) NULL,
    `pad` VARCHAR(60) NULL
) ENGINE=OLAP
UNIQUE KEY(`id`)
COMMENT 'OLAP'
DISTRIBUTED BY HASH(`id`) BUCKETS 3;

CREATE TABLE IF NOT EXISTS `sbtest7` (
    `id` INT NOT NULL,
    `k` INT NULL,
    `c` VARCHAR(120) NULL,
    `pad` VARCHAR(60) NULL
) ENGINE=OLAP
UNIQUE KEY(`id`)
COMMENT 'OLAP'
DISTRIBUTED BY HASH(`id`) BUCKETS 3;

CREATE TABLE IF NOT EXISTS `sbtest8` (
    `id` INT NOT NULL,
    `k` INT NULL,
    `c` VARCHAR(120) NULL,
    `pad` VARCHAR(60) NULL
) ENGINE=OLAP
UNIQUE KEY(`id`)
COMMENT 'OLAP'
DISTRIBUTED BY HASH(`id`) BUCKETS 3;
